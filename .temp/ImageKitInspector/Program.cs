using System; using System.Reflection; class Program { static void Main() { foreach(var t in typeof(Imagekit.ServerImagekit).Assembly.GetExportedTypes()) { if(t.Name == "FileCreateRequest" || t.Name == "Result" || t.Name == "GetFileListRequest" || t.Name == "ResultList") { Console.WriteLine("Type: " + t.FullName); foreach(var p in t.GetProperties()) Console.WriteLine("  Prop: " + p.Name + " (" + p.PropertyType.Name + ")"); } } } }

# دليل التكامل: إعادة إرسال رمز التحقق للمندوب (Resend OTP)

هذا الملف يوضح لمطور تطبيق الموبايل الخاص بالمندوب كيفية استخدام نقطة الاتصال (Endpoint) الجديدة الخاصة بإعادة إرسال رمز التحقق (OTP) في حال لم يصل الرمز للعميل أو للتاجر.

## نقطة الاتصال (Endpoint)

**المسار:**  
`POST /api/drivers/assignments/{assignmentId}/resend-otp`

**الوصف:**  
تستخدم هذه النقطة لطلب إعادة إرسال كود التحقق الجديد (OTP). 
- في حالة الاستلام من المتجر (`pickup`)، سيتم إرسال الرمز الجديد كإشعار إلى التاجر.
- في حالة التسليم للعميل (`delivery`)، سيتم إرسال الرمز الجديد كرسالة (أو إشعار) إلى العميل.

**التصريح (Authorization):**  
تتطلب إرسال توكن المندوب في الهيدر (`Bearer Token`).

---

## الطلب (Request)

**النوع:** `application/json`

**جسم الطلب (Body):**
```json
{
  "otpType": "pickup" // يمكن أن يكون "pickup" أو "delivery"
}
```

- **`otpType`**: 
  - أرسل `"pickup"` إذا كان المندوب في المتجر ويحتاج التاجر لكود الاستلام مرة أخرى.
  - أرسل `"delivery"` إذا كان المندوب عند العميل ويحتاج العميل لكود الاستلام مرة أخرى.

---

## الردود (Responses)

### 1. الرد الناجح (200 OK)
عند نجاح العملية، سيقوم السيرفر بإرسال الكود للطرف الآخر بنجاح، وسيعيد لك هذا الرد لتظهره للمندوب:

```json
{
  "message_ar": "تم إرسال رمز التحقق بنجاح",
  "message_en": "OTP resent successfully"
}
```

### 2. خطأ: الرمز تم تأكيده مسبقاً (400 Bad Request)
إذا حاول المندوب إعادة إرسال الكود بعد أن تم إدخاله وتأكيده مسبقاً.

```json
{
  "error_code": "PICKUP_OTP_ALREADY_VERIFIED",
  "message": "Pickup OTP has already been verified."
}
```
*(يظهر رمز `DELIVERY_OTP_ALREADY_VERIFIED` في حالة طلب العميل)*

### 3. خطأ: الطلب لا يخص هذا المندوب (400 Bad Request)
إذا حاول المندوب إعادة إرسال كود لطلب غير مسند إليه حالياً.

```json
{
  "error_code": "ASSIGNMENT_NOT_OWNED",
  "message": "You can only resend OTP for your assigned deliveries."
}
```

---

## ملاحظات لمطور الموبايل لتقديم أفضل تجربة مستخدم (UX):
1. **منع التكرار المزعج (Cooldown Timer):** يُفضل إضافة مؤقت زمني (مثلاً 60 ثانية) على زر "إعادة إرسال الرمز" بعد الضغط عليه؛ لمنع المندوب من إرسال طلبات متعددة متتالية بالخطأ.
2. **شاشة الاستلام من المتجر (Pickup):** يجب أن يُستدعى الـ API برقم الـ `assignmentId` ونوع `"otpType": "pickup"`.
3. **شاشة التسليم للعميل (Delivery):** يجب أن يُستدعى الـ API بنفس الـ `assignmentId` ولكن بنوع `"otpType": "delivery"`.

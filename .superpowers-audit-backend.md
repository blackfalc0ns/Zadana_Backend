## Worktree vs main

`F:/Zadna/Zadana_Backend/.worktrees/eastern-proximity-dispatch` is **not present** (empty / missing). Main is **not behind a worktree** â€” proximity work was never landed.

On main: plan exists at `docs/superpowers/plans/2026-08-19-eastern-proximity-dispatch.md` (all tasks still `[ ]`). Planned files `GeoDistance.cs`, `DeliveryProximityLimits.cs`, `NearestBranchSelector.cs` **do not exist**. Live code still city-locks Dammam vs Khobar vs Dhahran.

---

## Findings (25)

- **id:** `GAP-01`  
  **area:** Delivery/dispatch  
  **severity:** Critical  
  **title:** Eastern metro still city-locked; Dammam driver cannot take Khobar pickup  
  **evidence:** `DeliveryDispatchService.cs:650-653` (`pickup-customer-city-mismatch` returns null); `DeliveryPickupAreaMatcher.cs:11-23`; accept path `DeliveryDispatchService.cs:380-393` (`DELIVERY_OFFER_CITY_MISMATCH`); admin `AdminOrdersController.cs:181-186` (`DRIVER_CITY_MISMATCH`); test `DeliveryDispatchServiceTests.cs:293-316` asserts Dammam driver is **not** offered for Khobar.  
  **impact:** Eastern metro (Dammam/Dhahran/Khobar) cannot share couriers. Dispatch stalls even with fresh GPS nearby.  
  **suggested next step:** Implement the 50 km GPS plan; stop treating city strings as a hard lock when coordinates exist.

- **id:** `GAP-02`  
  **area:** Checkout/cart geography  
  **severity:** Critical  
  **title:** Branch pick is same-city + `DeliveryRadiusKm`, not 50 km GPS  
  **evidence:** `CartBranchSelectionSupport.cs:78-104` (same city first, then `DeliveryRadiusKm`); `CheckoutSupport.cs:258-266` and `945-964` (same-city early return; empty if city set and different); `CheckoutSupport.cs:1252-1270` (same-city **bypasses** radius). Plan: ignore `DeliveryRadiusKm`, cutoff `MaxMatchKm = 50`.  
  **impact:** Khobar customer can be bound to a far same-city branch, or a nearby Dhahran branch is dropped. Cart vs checkout can disagree.  
  **suggested next step:** Shared `NearestBranchSelector` with 50 km Haversine; city only as display, not lock.

- **id:** `GAP-03`  
  **area:** Dispatch GPS  
  **severity:** Critical  
  **title:** Stale/missing GPS and >50 km are scored, not excluded  
  **evidence:** `DeliveryDispatchScoring.GpsFreshnessThreshold` = 5 min (`:29`); stale GPS gets `freshnessPenalty = 80` (`:93`) but stays eligible; `ResolveDistanceKm` (`:181-213`) falls back to zone+5 km or `99m`; `ResolveTier` still yields `out-of-area-low-priority` (`:159-178`). No `DeliveryProximityLimits`.  
  **impact:** After city lock is removed, far/stale drivers can still win. Today GPS is a score, not a gate.  
  **suggested next step:** Hard-exclude missing/stale GPS and distance > 50 km when pickup coords exist.

- **id:** `GAP-04`  
  **area:** Driver geography / unfinished proximity  
  **severity:** Important  
  **title:** Driver signup/profile still require city; going online says â€œchoose the cityâ€  
  **evidence:** `RegisterDriverCommandHandler.cs:37-48` (`DRIVER_SERVICE_AREA_REQUIRED`); `OperationalGeographyScope.EnsureDriverServiceAreaAsync:69-75` still calls city-required `EnsureOperationalRegionCityAsync`; `Driver.HasServiceArea:48-50` needs Region **and** City; `DriverProfileReadinessFactory.cs:74-76` (`missing_region_city`); `UpdateDriverAvailabilityCommand.cs:55-56`.  
  **impact:** Blocks region-only Eastern signup in the plan; drivers without a city cannot go online.  
  **suggested next step:** Region-only `EnsureDriverServiceAreaAsync`; `HasServiceArea` = region; readiness key `missing_region`.

- **id:** `GAP-05`  
  **area:** Geography matchers  
  **severity:** Important  
  **title:** Three city normalizers; Dammam/Khobar/Dhahran stay distinct  
  **evidence:** `DeliveryCityMatcher.cs:31-35`; `CheckoutSupport.IsSameCityDelivery/NormalizeCityKey:1273-1299` (cart uses matcher, checkout uses its own); dead `DeliveryDispatchScoring.NormalizeCityForDispatch:246-283` (never called). Cart uses **Haversine** (`CartBranchSelectionSupport.cs:139-152`); checkout/dispatch use **equirectangular** (`CheckoutSupport.cs:1237-1247`, `DeliveryDispatchScoring.cs:146-156`).  
  **impact:** Same address can match in cart and fail in checkout (or the reverse). Distances disagree.  
  **suggested next step:** One `GeoDistance` + one city/metro matcher; delete dead `NormalizeCityForDispatch`.

- **id:** `GAP-06`  
  **area:** Pickup branches contract  
  **severity:** Important  
  **title:** Pickup APIs still filter by city string, not 50 km  
  **evidence:** `CheckoutSupport.GetPickupBranchesForCityAsync:1582-1584`; contract `CUSTOMER_APP_CONTRACTS/CHECKOUT_PICKUP_BRANCHES_BY_CITY_MOBILE_HANDOFF_AR.md` (city-only list, `PICKUP_CITY_REQUIRED`).  
  **impact:** Metro pickup from a neighboring Eastern city is hidden even if the branch is 15 km away.  
  **suggested next step:** After proximity lands, update this handoff to GPS+50 km (keep city as optional hint).

- **id:** `GAP-07`  
  **area:** Auth/OTP  
  **severity:** Important  
  **title:** Registration OTP is email-only; WhatsApp/SMS never sent  
  **evidence:** `RegisterCustomerCommandHandler.cs:75-78`, `RegisterDriverCommandHandler.cs:93-96`, `RegisterVendorCommandHandler.cs:101`, `ResendOtpCommandHandler.cs:206` â€” all `SendOtpEmailAsync` only. `appsettings.json` `WapilotOtp.Enabled: false`, `WhatsAppCloudOtp.Enabled: false`, `DefaultCountryCode: "+20"` (Egypt, not `+966`). `IOtpService.SendOtpSmsAsync` unused on register.  
  **impact:** Drivers/customers without working email never verify. Phone-centric apps wait on a channel the backend does not use.  
  **suggested next step:** Dual-channel OTP (email + WhatsApp) for drivers/customers; KSA default country code.

- **id:** `GAP-08`  
  **area:** Auth security  
  **severity:** Important  
  **title:** Wapilot webhook is open when secret is empty  
  **evidence:** `WapilotWebhooksController.IsWebhookSecretValid:135-140` returns `true` if `WebhookSecret` blank; committed `appsettings.json:70` `"WebhookSecret": ""`. `[AllowAnonymous]` POST.  
  **impact:** Anyone can POST webhook noise/logs; if delivery status is ever wired to this endpoint, it is unauthenticated.  
  **suggested next step:** Fail closed when secret missing; add to production required settings.

- **id:** `GAP-09`  
  **area:** Auth security  
  **severity:** Important  
  **title:** BotChallenge is a no-op without secret; not required in production  
  **evidence:** `BotChallengeAttribute.cs:26-29`; `TurnstileBotChallengeService.cs:33-34`; `Program.cs:100-110` required keys omit `BotChallenge:SecretKey`. Register endpoints still tagged `[BotChallenge]`.  
  **impact:** Production can ship open register/forgot-password without CAPTCHA if the env key is forgotten.  
  **suggested next step:** Require Turnstile in Production the same way JWT/Moyasar secrets are required.

- **id:** `GAP-10`  
  **area:** Auth/registration  
  **severity:** Important  
  **title:** Emails restricted to `.com` TLD  
  **evidence:** `EmailValidationRules.HasComTopLevelDomain:7-9`; used by `RegisterCustomerCommandValidator`, `RegisterDriverCommandValidator`, `RegisterVendorCommandValidator`, `VerifyOtpCommandValidator`.  
  **impact:** `.sa`, `.net`, corporate domains cannot register.  
  **suggested next step:** RFC email validation; drop `.com` lock.

- **id:** `GAP-11`  
  **area:** Auth reliability  
  **severity:** Important  
  **title:** Pending registration is cache+JWT only after DB table drop  
  **evidence:** `PendingRegistrationService.cs:19-22, 43, 90`; migration `20260726163201_DropPendingRegistrations.cs`; `appsettings.json` Redis `ConnectionString: ""`.  
  **impact:** Redis flush, restart without Redis, or multi-instance memory cache drops in-flight OTP sessions (24h JWT still exists but cache lookup can fail).  
  **suggested next step:** Durable pending session (Redis required in all shared envs, or re-persist hashed OTP).

- **id:** `GAP-12`  
  **area:** Payments/payouts  
  **severity:** Important  
  **title:** Automated Moyasar payouts off; default payout city is Riyadh  
  **evidence:** `appsettings.json:111-120` `Payouts.Enabled: false`, `DefaultCity: "Riyadh"` vs Eastern ops. Live path is `PayoutOrchestrator` (~2207 lines) + admin manual confirm in `AdminPayoutsController`.  
  **impact:** Vendor/driver payouts are manual; mis-set city metadata if automation is flipped on.  
  **suggested next step:** Treat manual dual-control as the product path until Enabled+SourceId are production-gated; Eastern default city.

- **id:** `GAP-13`  
  **area:** Payments/refunds  
  **severity:** Important  
  **title:** `CreateRefundCommand` has no handler (dead payment command)  
  **evidence:** `CreateRefundCommand.cs:7-10` is `IRequest<Guid>` with validator only; **no** `IRequestHandler<CreateRefundCommand>`. Admin refunds go through support-case `AdminOrdersController.CreateRefund:378-419`.  
  **impact:** Callers/tests expecting MediatR refunds fail; two mental models for refunds.  
  **suggested next step:** Delete the orphan command or implement it as the single gateway to `OrderSupportCaseWorkflowService`.

- **id:** `GAP-14`  
  **area:** COD  
  **severity:** Important  
  **title:** COD owed â‰¥ 500 SAR silently empties the driver pool  
  **evidence:** `FinancialSettingsOptions.DriverCodBlockThresholdAmount` default `500m` (`:38`) â€” **not** in committed `appsettings.json` `FinancialSettings`; `DriverCodEnforcementService.cs:36-48`; dispatch note `cod-balance-blocked` (`DeliveryDispatchService.cs:735`).  
  **impact:** Ops cannot see/tune the threshold in config; COD orders can sit with â€œno driverâ€ while drivers are blocked.  
  **suggested next step:** Put threshold in `appsettings` + admin UI; surface `cod-balance-blocked` on the order.

- **id:** `GAP-15`  
  **area:** Payments inbox  
  **severity:** Important  
  **title:** Payment webhook inbox gives up after 8 attempts with no poison-queue alert  
  **evidence:** `PaymentProviderEventInboxWorker.cs:19, 68-78` (`MaxAttempts = 8`); failed rows older than retry window are skipped. Host `BackgroundServiceExceptionBehavior.Ignore` (`Program.cs:67-70`).  
  **impact:** Paid Moyasar events can stick `Failed` forever; customer paid, order unconfirmed.  
  **suggested next step:** Admin poison queue + alert after max attempts; do not Ignore-crash the worker without a health metric.

- **id:** `GAP-16`  
  **area:** Realtime/OneSignal  
  **severity:** Important  
  **title:** Pickup reminder/expired/extended never hit OneSignal or SMS  
  **evidence:** `PickupNoShowWorker.cs:265-276, 295, 335` â€” inbox via `SendToUserAsync` + `TODO(pickup-sms)`; `NotificationService.SendToUserAsync:62-109` is persist + SignalR only (no push). Contract `CUSTOMER_APP_CONTRACTS/NOTIFICATIONS_CONTRACT.md:70-80` lists `pickup_reminder`, `pickup_expired`, etc.  
  **impact:** Backgrounded customer apps miss pickup window; SMS never sent.  
  **suggested next step:** Route pickup worker through `OrderStatusNotificationDispatcher` / OneSignal heads-up; then SMS.

- **id:** `GAP-17`  
  **area:** Realtime/OneSignal  
  **severity:** Important  
  **title:** Order-status push uses Warning diagnostics and skips when â€œforegroundâ€  
  **evidence:** `OrderStatusNotificationDispatcher.cs:101-127` (`[PUSH-DIAG]` `LogWarning`; suppress if `ICustomerPresenceService.IsOnline`). Presence is in-process (`CustomerPresenceSweepWorker`).  
  **impact:** Multi-instance: presence is local, so one node thinks the user is online and drops push. Log spam in prod.  
  **suggested next step:** Shared presence (Redis); drop Warning diagnostics; only suppress if this instance **and** a recent SignalR ack.

- **id:** `GAP-18`  
  **area:** Admin vs mobile contracts  
  **severity:** Important  
  **title:** Driver profile contract still documents zones; API uses region/city  
  **evidence:** `DRIVER_APP_CONTRACTS/PROFILE_CONTRACT.md:37-38, 80-89, 115-120` (`primaryZoneId`, `missing_zone_selection`); live `DriverProfileDto:112-125` (`Region`/`City`); readiness `missing_region_city`. `OPERATIONAL_GEOGRAPHY_HANDOFF_AR.md:30-34` still requires `"city": "DAMMAM"`.  
  **impact:** Driver app onboarding against stale contract (zones) vs geography handoff (cities) vs planned region-only.  
  **suggested next step:** One driver geography contract: region-only; delete `primaryZoneId` / `missing_zone_selection`.

- **id:** `GAP-19`  
  **area:** Customer contracts  
  **severity:** Minor  
  **title:** Notifications contract samples `dataObject: null`; API returns `DataObject`  
  **evidence:** `CUSTOMER_APP_CONTRACTS/NOTIFICATIONS_CONTRACT.md:39, 94` vs `NotificationsController.cs:148, 170` and `CUSTOMER_APP_CONTRACTS/SIGNALR_CONTRACT.md:174-182` (â€œuse `dataObject`â€).  
  **impact:** Mobile parsers that follow the notifications sample ignore structured payload.  
  **suggested next step:** Align samples with live DTO (`dataObject` parsed JSON).

- **id:** `GAP-20`  
  **area:** Tests  
  **severity:** Important  
  **title:** No KnownIssues; tests freeze city-lock as correct; proximity untested  
  **evidence:** No `KnownIssue` / `Fact(Skip)` files. `DeliveryDispatchServiceTests.TryAutoDispatchAsync_WhenOnlyDriverInSameRegionDifferentCity_ShouldNotOfferDriver:293`. `GetCheckoutSummaryQueryHandlerTests.Handle_WhenAddressIsInSameCity_DoesNotRejectDeliveryBecauseOfRadius:218`. Planned `GeoDistanceTests` not in repo.  
  **impact:** Implementing 50 km metro **fails existing tests** unless they are rewritten first. Zero coverage of the intended matcher.  
  **suggested next step:** Invert those tests to metro+50 km **before** production code (as in the plan).

- **id:** `GAP-21`  
  **area:** Tech debt / tests  
  **severity:** Minor  
  **title:** Duplicated test payment resolver and duplicated city/distance helpers  
  **evidence:** `tests/Zadana.Application.Tests/Helpers/TestPaymentGatewayResolver.cs` and `tests/Zadana.UnitTests/TestHelpers/TestPaymentGatewayResolver.cs` (near-identical). Production twins: `CheckoutSupport` vs `CartBranchSelectionSupport` vs `DeliveryCityMatcher`.  
  **impact:** Fixes land in one copy; the other keeps old behavior.  
  **suggested next step:** One shared test helper; one shared geo module.

- **id:** `GAP-22`  
  **area:** Tech debt  
  **severity:** Important  
  **title:** God files concentrate checkout, support, payouts, email  
  **evidence:** `EmailCenterService.cs` ~2889 lines; `OrderSupportCaseWorkflowService.cs` ~2690; `PayoutOrchestrator.cs` ~2207; `VendorAccessController.cs` ~1955; `CheckoutSupport.cs` ~1886; `HomeReadService.cs` ~1293 (swallows SQL â€œinvalid objectâ€ at `:1091-1103`).  
  **impact:** High regression risk; home can look â€œemptyâ€ when migrations are behind.  
  **suggested next step:** Split by use case; fail home health if schema mismatch instead of swallowing.

- **id:** `GAP-23`  
  **area:** Migrations  
  **severity:** Important  
  **title:** Two EF migration folders in one assembly + repair SQL + committed `migration.sql`  
  **evidence:** Canonical `src/Zadana.Infrastructure/Migrations` (snapshot `ApplicationDbContextModelSnapshot.cs`) **and** `src/Zadana.Infrastructure/Persistence/Migrations` (94 files, namespace `Persistence.Migrations`, still `Migration` subclasses). `20260601000000_RepairMissingCriticalTables.cs:10-21` IF-NOT-EXISTS table creates. `src/Zadana.Api/migration.sql` (~full schema script).  
  **impact:** `dotnet ef` can see leftover IDs; repair migration masks drift; `migration.sql` can be applied out of band and desync history.  
  **suggested next step:** Exclude/archive `Persistence/Migrations`; never apply `migration.sql` on a database that uses EF history.

- **id:** `GAP-24`  
  **area:** Ops/health  
  **severity:** Important  
  **title:** `/health` and `/health/ready` do not check SQL, Moyasar, or OneSignal  
  **evidence:** `Program.cs:928-932` only `RedisDistributedCacheHealthCheck`; Redis is healthy when not required (`RedisDistributedCacheHealthCheck`); `/health/ready` tags `ready` = cache only (`:1267-1270`). `MapPrometheusScrapingEndpoint()` unauthenticated (`:1273-1275`).  
  **impact:** Load balancer can keep a node with a dead SQL Server. Metrics exposed if the proxy is open.  
  **suggested next step:** Add SQL health to `ready`; restrict `/metrics`; optionally ping Moyasar/OneSignal as `live` not `ready`.

- **id:** `GAP-25`  
  **area:** Ops/jobs  
  **severity:** Important  
  **title:** Dispatch worker alert cooldown is in-memory; worker exceptions are ignored  
  **evidence:** `DeliveryDispatchWorker._lastAlertSentAtUtc:35`; `Program.cs:67-70` `BackgroundServiceExceptionBehavior.Ignore`; worker comments `:208-212` (logging can throw on shutdown).  
  **impact:** Restart resets â€œno driverâ€ admin-alert cooldown (spam) or, after Ignore, a crashed worker never restarts. Orders sit in `DriverAssignmentInProgress`.  
  **suggested next step:** Persist last-alert timestamp; `StopHost` on dispatch/payment workers; add a â€œdispatch worker heartbeatâ€ health check.

---

### Not in the top 25 (still real)

- Google OAuth `ClientId` is committed (`appsettings.json:22`) â€” public-by-design, not a secret.  
- Assignment OTPs use `Random.Shared` (`DeliveryAssignment.cs:325-326`) â€” weak vs `RandomNumberGenerator`.  
- Pickup OTP is correctly omitted from the shared order group (`OrderTrackingRealtimeNotifier.cs:97`) and sent only on the customer user group.
